# Core runtime dependencies
numpy>=1.24.0
pygame>=2.5.0
matplotlib>=3.7.0

# GPU acceleration (CUDA 12.x - adjust for your CUDA version)
# CUDA 13.x: cupy-cuda13x>=13.6.0
# CUDA 12.x (RTX 30/40 series): cupy-cuda12x>=13.0.0
# CUDA 11.x: cupy-cuda11x>=13.0.0
# CPU fallback (no GPU): cupy-cuda12x (works without CUDA, slower)

# Optional: video export (OpenCV)
opencv-python>=4.8.0

# Development dependencies (use: pip install -e ".[dev]")
# pytest>=7.4.0
# pytest-cov>=4.1.0
# black>=23.0.0
